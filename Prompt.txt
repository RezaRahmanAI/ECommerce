You are a principal-level software architect with deep expertise in API design, database optimization, caching strategies, and full-stack performance engineering.
Task: Audit and restructure my project's API layer and views for maximum performance and scalability.
Hard constraints:

Preserve all existing business logic exactly — do not change what the code does, only how it does it
Do not alter data models or database schemas unless strictly necessary for a performance fix

Focus areas:

API layer — eliminate N+1 queries, add pagination, optimize serialization, reduce payload size, apply proper HTTP caching headers
Database queries — use select_related/prefetch_related (or equivalent), add missing indexes, batch operations where possible
View layer — remove redundant computations, push heavy logic to the DB or background tasks, cache expensive results
Architecture — suggest and apply structural improvements (e.g. service layer, query objects, response caching) that reduce latency

Output format: For each change, provide:

What was changed and where (file/function)
Why it improves performance
The refactored code